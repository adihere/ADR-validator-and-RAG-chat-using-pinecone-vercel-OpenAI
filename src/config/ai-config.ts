export const AI_CONFIG = {
  models: {
    chat: "gpt-4", // Default chat model
    validation: "gpt-4", // Model for ADR validation
    creation: "gpt-4", // Model for ADR creation
  },
  temperature: 0.7,
  maxTokens: 4000,
  // Add other OpenAI-related configurations here
};